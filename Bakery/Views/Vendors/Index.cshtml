@{ Layout = "_Layout"; }
@using Bakery
<ul>
    @foreach (Vendor vendor in Model)
    {
        <li><a href="vendors/@vendor.Id">@vendor.Name - @vendor.Description</a></li>
    }
</ul>
<a href="/vendors/new">Add vendor</a>